# 第 1 天报告（立项书）：通用游戏智能体（课题七 · 腾讯 IEG 课题）

## 基本信息

- **课题编号与名称**：课题七：通用游戏智能体（腾讯 IEG 课题）
- **完成方式**：独立完成（占 1 组），本人一人承担需求、实现、测试与演示；联络人即本人。

## 今日目标

确定 9 天内可交付的最小范围：选定课题的使用场景与课内交付边界、MVP 必做与不做清单、主路径与验收口径、主要风险与应对；并利用开课前的准备时间提前完成环境搭建与官方权重推理验证，降低后续阻塞风险。

## 课题与使用场景

**选用理由**：NitroGen（NVIDIA 开源，arXiv:2601.02427）是目前唯一开源的通用游戏智能体基础模型（视觉→手柄动作，4 万小时/1000+ 游戏数据训练），数据集与预训练权重均可获取，能在课内完成"zero-shot 推理 + 单游戏定量评估"的完整闭环。

**使用场景**：为游戏 AI 方向的研究者提供一套跨游戏 zero-shot 操控基线的最小评测与可视化示例。

**课内交付边界**：模型输入采用与数据集对应的离线游戏画面，**不接入真实商业游戏做实时闭环控制**；第 5 天演示以离线评估的模型输出与标注对照（表格/曲线）为主，辅以录屏说明。

## 必做与不做

### 必做（9 天结束时可演示、可验收）

1. **跑通官方推理**：加载官方 ng.pt 权重完成端到端推理，README 含环境、依赖版本与命令，他人可复现。
2. **单游戏标注统计与可视化**：下载同一款游戏 ≥ 500 帧手柄标注，统计 16 维按键触发频率分布与摇杆取值分布（直方图/热力图），可视化 ≥ 10 条连续动作序列曲线。
3. **定量评估**：划分 ≥ 200 帧测试集，报告按键准确率（逐帧逐键一致率）与摇杆误差（MSE 与 Pearson 相关系数）。
4. **zero-shot 基线达标**：按键准确率 ≥ 50%，摇杆相关系数 ≥ 0.4；未达标则记录差距并分析原因。
5. **第 5 天演示**：模型输出与标注真值的逐帧对比（表格或录屏），演示说明可复现。
6. **归档交付**：归档代码与指标表；实验说明写入结课大报告（建议 3000 字以上，可与结课大报告合并撰写）。

### 数据范围与下载限定

数据集 nvidia/NitroGen 共 100 个分片（约 165 GB），**不得下载全库**。本课题仅下载 1 个分片（SHARD_0034，约 1.33 GB，占 1%），从中按 `metadata.json` 的 `game` 字段提取 **Hollow Knight** 标注。选型依据（实测）：该分片内 Hollow Knight 共 1,444,200 帧、1439 个 chunk、6 个视频；随机抽样 30 个 chunk 实测按键触发率 26.0%、左摇杆移动率 39.7%、完全 IDLE 帧占比 49.2%；按键分布与游戏机制吻合（跳跃键最高 11.0%、攻击 5.3%、冲刺 3.3%），标注质量与数据量均满足要求。备选：super_mario_64（899,400 帧）、hades（538,200 帧）。

### 不做

- 不下载全库、不处理视频数据本身（课程数据集不含视频，评估基于 Parquet 标注与模型输出）。
- 不从零预训练或大规模微调（算力不允许，且非课题要求）。
- 不搭建 universal simulator、不接真实游戏实时闭环（见课内交付边界）。
- 不做语言条件控制、强化学习后训练等论文展望方向。
- 不将权重与数据用于商业用途（CC BY-NC 4.0）。

### 可选扩展（单列一条）

**可视化工具**：批量导出 ≥ 20 段手柄动作曲线（按键时序 + 摇杆轨迹），自动标出模型输出与标注差异最大的 5 帧（按按键不一致位数 + 摇杆 L2 误差排序）。
**算力来源**：曲线绘制与差异排序为纯 CPU 计算，用本地 PC 完成；模型推理用本地 GPU（RTX 5060，8GB，已实测单帧推理约 1 秒，满足 200 帧测试集需求），必要时申请机房算力补充。

## 主路径与验收

**主路径**：按 README 复现环境 → 运行冒烟测试加载 ng.pt → 从单分片提取 Hollow Knight ≥ 500 帧标注 → 统计按键/摇杆分布并出图 → 划分 ≥ 200 帧测试集逐帧推理 → 与标注对齐计算按键准确率与摇杆 MSE/相关系数 → 产出指标表与对比可视化（第 5 天演示）→ 归档代码与指标表，实验说明并入结课大报告。

**验收说明**：

1. 在干净 Windows + NVIDIA GPU 环境下 → 按 README 安装依赖并运行 `scripts\smoke_test.py` → 应看到 `SMOKE TEST PASSED`（模型加载约 11 秒、单帧推理约 1 秒，输出 18×21 按键与左/右摇杆坐标的动作块）。
2. 在数据提取完成后 → 运行统计脚本 → 应看到 ≥ 500 帧 Hollow Knight 标注的按键频率表与摇杆分布图，且按键分布与游戏机制吻合（跳跃键 south 触发率最高）。
3. 在测试集评估完成后 → 查看指标表 → 全帧口径与过滤 IDLE 口径两组指标均已报告，且按键准确率 ≥ 50%、左摇杆相关系数 ≥ 0.4；若未达标，报告含差距量化与原因分析（标注延迟、IDLE 占比、类型分布偏差）。

**九天进度计划**（对照指导书 2.2 进度安排）：

| 天 | 当日任务 | 本课题具体内容 | 可核对产出 |
| --- | --- | --- | --- |
| 第 1 天 | 选题确认，范围与验收 | 提交本报告；环境与权重已就绪 | 本报告 + Git 提交 |
| 第 2 天 | 方案组成与技术选型 | 精读论文与推理代码，确定动作对齐关系与指标口径、数据管线选型 | 选型说明 |
| 第 3 天 | 主路径数据与调用约定 | 提取 Hollow Knight ≥ 500 帧标注；定接口约定；启动项目备忘 | 数据提取脚本 + 备忘初稿 |
| 第 4 天 | 工程起步，能按说明运行 | 可复现 README 定稿；按键/摇杆分布统计 + ≥ 10 条序列可视化 | README + 统计表 + 曲线图 |
| 第 5 天 | 主路径贯通，可演示 | ≥ 200 帧测试集评估；演示模型输出 vs 标注对照 | 指标表 + 演示材料 |
| 第 6 天 | 推进两块可验证工作 | ① 扩展可视化工具（≥ 20 段曲线 + 差异 Top-5 帧）② 基线核对 | 工具 + 曲线图 + 自测记录 |
| 第 7 天 | 贯通验证与排错 | 全流程通检；基线不达标则误差分析与重估 | 通检表 + 误差分析 |
| 第 8 天 | 回归与交付准备 | 回归测试；他人可跟做启动说明；对照必做自测表 | 启动说明 + 自测对照表 |
| 第 9 天 | 结课总结 | 结课大报告（实验说明建议 ≥ 3000 字合并撰写） | 结课大报告 |

每天按指导书 6.1 的通用栏目提交当日小报告（基本信息、今日目标、当日必写栏目、今日完成与自检、问题、计划与 AI 沟通），共 9 份；远程 Git 仓库每日提交留痕。

## 风险与依赖

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 环境依赖冲突（官方代码未锁 transformers 版本） | 阻塞推理 | **已解决**：transformers 5.x 存在 SigLIP 破坏性变更，已锁定 4.57.1 并写入 README |
| 本地 GPU 显存（8GB） | 推理慢或 OOM | 已实测可行；必要时减小批量或申请机房算力 |
| 数据集无游戏索引（游戏名在分片内部） | 选型与提取困难 | **已解决**：下载单分片读取 metadata 完成选型；提取脚本按 game 字段过滤 |
| 字段口径差异（数据集 17 键含 guide，论文正文写 16 维） | 指标口径争议 | 评估时注明口径（16 维对齐论文或 17 维全量，分键报告） |
| 基线不达标 | MVP 第 4 条失败 | IDLE 帧约 49% 会干扰指标解读，同时报告过滤前后两组；排查标注延迟对齐 |
| 每日报告与提交遗漏 | 缺日常考核 | 当天实践结束即提交报告与 Git 提交，不隔夜 |

## 今日完成与自检

**已完成**：

1. 立项范围全部确定（本报告）。
2. 环境搭建：Python 3.10 虚拟环境 + PyTorch 2.11（CUDA 12.8，适配 RTX 5060）+ 全部推理依赖；修复 transformers 兼容性问题。
3. 官方 ng.pt 权重（1.84 GB）下载并完成端到端推理冒烟测试。
4. 单分片 SHARD_0034（1.33 GB）下载，完成 Hollow Knight 数据验证与选型（见"数据范围"节实测数据）。
5. README 可复现说明与冒烟测试脚本已提交。

**对照验收自检**：验收说明 1 已可核对（冒烟测试通过）；验收说明 2、3 尚空着——数据提取统计脚本与评估流程为后续工作（按计划分别于第 3~5 天完成）。

## 问题、计划与 AI 沟通

**不成功的沟通**：

1. 初次提取实践指导书 PDF 文本时文字层大面积乱码，直接让 AI 解读乱码内容不可行；后改为换用另一解析库重新提取并渲染页面图像，才获得可读全文。教训：源数据不可读时应先解决提取方式，而非让模型猜。
2. 在 PowerShell 中用多行 `python -c "..."` 传长脚本时，因引号与换行解析错误反复报 NameError；后改为把代码写入临时脚本文件再执行，一次通过。

**比较有效的沟通**：

1. 直接粘贴完整报错 `AttributeError: 'SiglipVisionModel' object has no attribute 'vision_model'` 及版本环境信息，AI 准确判定为 transformers 5.x 破坏性变更，给出降级到 4.57.1 的方案，验证有效——完整报错上下文比描述症状有效得多。
2. 给出"不得下载数据集全库"的硬约束后，AI 采用"仅下载最小分片 + 读 tar 内 metadata 统计游戏帧数"的方案，在不违规的前提下完成了选型验证——明确约束反而促成了更合理的方案。


