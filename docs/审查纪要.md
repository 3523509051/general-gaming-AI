# 审查纪要（第 4 天 · 工程起步）

## 审查人 / 时间 / 范围

| 项 | 内容 |
| --- | --- |
| 审查人 | 廖晓泽（人） + AI 协作（code-explorer 子代理通读） |
| 审查时间 | 2026-08-22 |
| 审查分支 | main（工作区未提交改动） |
| 审查范围 | `scripts/` 全部业务脚本 + `web/` 前端 + `start_web.ps1/bat`，共 15 个文件 |

## 审查检查项与结论

### 1. 目录合理性
- `scripts/` 全为 Python 数据流水线 + Flask 后端，`web/` 全为前端静态资源（原生 HTML/JS + 本地 ECharts，无 CDN），无巨型无关模板、无错误语言栈。✅
- 发现冗余：`evaluate_hades.py`（写死路径，被通用 `evaluate.py` 完全覆盖）、`build_hades_testset.py`（逻辑已并入 evaluate.py 的 `build_testset()`）——**已归档至 `scripts/archive/` 并在 .gitignore 忽略**。

### 2. 配置与密钥
- 全局检索 `token/password/secret/api_key/bearer/Authorization/sk-` 等，**scripts/ 与 web/ 均 0 命中**，无真实密钥入库。✅
- `.env` 已在 .gitignore 忽略（保留 `.env.example` 白名单）。

### 3. 与第 3 天书面约定一致
| 约定 | 代码位置 | 结论 |
| --- | --- | --- |
| `/api/frame` 血缘断言（game/video/frame 三方，不静默） | `app.py` `assert_lineage` | ✅ 一致 |
| 数据路径 `data/<game>/` | extract/evaluate/app/stats 各脚本 | ✅ 一致 |
| `evaluate.py --game/--video/--fps` | argparse + app.py 调用 + README | ✅ 一致 |
| 差异定义 `D = 按键位数 + 双摇杆平均 L2/√2` | `app.py` `diff_score` | ✅ 一致 |

### 4. 依赖可安装 / 依赖幻觉
- 所有 import（flask/polars/pandas/matplotlib/numpy/PIL/pyarrow/imageio_ffmpeg/yt_dlp/nitrogen）均为真实存在且 venv 已实测安装的包，**无不存在或过时接口**。✅
- 环境分工：torch/nitrogen/polars 走 venv python；yt-dlp 走系统 python（app.py probe/download 子进程），README 已注明前置。

### 5. 硬编码路径（跨机复现风险）
| 级别 | 原位置 | 处理 |
| --- | --- | --- |
| 高 | `evaluate.py:70` ffmpeg fallback 写死 `C:\Users\77889\...` | **已改**：优先 imageio_ffmpeg，其次 PATH，再搜常见安装位置 |
| 高 | `stats_viz.py:56` 写死 `D:\general-gaming-AI\data` | **已改**：基于 `Path(__file__)` 仓库根相对路径 |
| 高 | `evaluate_hades.py` / `build_hades_testset.py` 多处写死路径 | **已归档**（不进入提交，避免不可复现代码入库） |
| 中 | `app.js` Top-5 秒数用未赋值的 `state._fps` | **已改**：改用后端序列接口返回的 `second` |

### 6. 一致性问题
- `app.py` 引用的 5 个子脚本路径（extract_game/probe_videos/evaluate/stats_viz/plot_shift_scan）与实际文件名全部一致。✅
- `start_web.ps1` 调 app.py 路径正确（`-WorkingDirectory $root` + `scripts\app.py`）。✅

## 问题清单（是否已修）

| # | 问题 | 级别 | 是否已修 |
| --- | --- | --- | --- |
| 1 | evaluate.py ffmpeg 硬编码用户路径 | 高 | ✅ 已改 |
| 2 | stats_viz.py 硬编码仓库绝对路径 | 高 | ✅ 已改 |
| 3 | evaluate_hades.py / build_hades_testset.py 冗余 + 硬编码 | 高 | ✅ 已归档 |
| 4 | app.js `state._fps` 未赋值导致秒数可能不准 | 中 | ✅ 已改 |
| 5 | probe/download 依赖系统 python 装 yt-dlp | 中 | README 已注明前置 |
| 6 | start_web.ps1 停进程模糊匹配可能误杀 | 低 | 接受（本项目单实例） |

## 结论
全部高危问题已处理，审查通过，可进入提交。
