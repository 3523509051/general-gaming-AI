# 顶层依赖清单（NitroGen 零样本评估 + Web 平台）
# 用法：NitroGen\.venv\Scripts\python.exe -m pip install -r requirements.txt
#
# 注意：
# - torch / torchvision 不在此清单内：版本强依赖显卡架构（见 README "安装 PyTorch（显卡自适应）"，
#   用 scripts/install_torch.ps1 或按兼容表手动安装）。
# - transformers 必须锁 4.57.1（5.x 有 SiglipVisionModel 破坏性变更）。
# - nitrogen 来自官方仓库，须先在 NitroGen/ 内执行 pip install -e ".[serve]"。

# --- 官方推理运行时 ---
diffusers==0.39.0
einops==0.8.2
transformers==4.57.1
huggingface_hub>=0.20.0

# --- 分析与统计 ---
numpy==2.2.6
pandas==2.3.3
polars==1.43.2
pyarrow>=15.0.0
matplotlib>=3.8.0
pillow>=10.0.0

# --- Web 平台后端 ---
flask>=3.0.0

# --- 视频下载与抽帧 ---
yt-dlp>=2024.12.0          # 视频下载/探测（app.py download / probe_videos.py）
imageio-ffmpeg>=0.4.0      # evaluate.py 抽帧用 ffmpeg 后端
