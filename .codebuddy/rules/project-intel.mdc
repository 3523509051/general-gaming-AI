---
description: 项目共享情报：技术选型、接口约定、评估指标口径、实验结果、当前进度与遗留事项。供 AI 每次对话共享上下文，避免重复探索。
alwaysApply: true
enabled: true
updatedAt: 2026-08-24T22:10:00.000Z
---

# 项目共享情报（通用游戏智能体 · 课题七）

> 精简自 `项目备忘.md` / `README.md` / 第 5 天报告（均不入库，本文件随仓库共享）。

## 一、技术选型（已定案）

- 方案 A「官方同构 · 最小依赖」：polars + `InferenceSession` 直调 + `actions_raw.parquet` + matplotlib（入选）；方案 B（pandas + serve.py + plotly）为对照组未选。
- Python ≥ 3.12；transformers 锁 4.57.1；torch 按显卡自适应。

## 二、脚本职责（scripts/）

| 脚本 | 职责 |
| --- | --- |
| `app.py` | Flask 后端（Web 平台，/api/games、frame、stats、sequences、metrics、evaluate、download、upload_shard 等 10+ 接口；LineageError 血缘断言） |
| `evaluate.py` | 通用 zero-shot 评估：建测试集（默认 200 帧纯随机）→ 推理 → shift 0~17 扫描 → metrics.csv/predictions.csv → 写 eval_results.db → 补统计图。**写死 CKPT=NitroGen/ng.pt，暂不支持 --ckpt** |
| `extract_game.py` | 从切片提取游戏标注 → data/<game>/annotations.parquet（自动发现 data/shards/ 全部切片） |
| `scan_shard.py` | 识别切片 tar.gz → 游戏/视频/链接清单（games_scan.json；--merge 合并多切片） |
| `stats_viz.py` | 统计可视化（按键/摇杆分布、10 条序列；--video 按视频） |
| `plot_shift_scan.py` | shift 扫描曲线图（中文） |
| `init_db.py` | 建 eval_results.db + 种子 |
| `probe_videos.py` | 视频链接可用性探测（yt-dlp + oEmbed fallback） |
| `finetune.py` | 扩展 A 微调（新增，第 6 天）：load_model → 抽帧对齐 → tokenizer.encode → model.forward → 保存 ng_finetuned.pt。**未验证完成，不提交** |
| `smoke_test.py` | ng.pt 冒烟测试（SMOKE TEST PASSED 即通过） |
| `install_torch.ps1` / `start_web.ps1/bat` | 显卡自适应装 PyTorch / 一键启动 Web |

## 三、数据形态与接口约定（踩过坑，勿再踩）

- 模型输出：18 步动作块 × 21 键 + 左右摇杆 (x,y)；`InferenceSession.predict(img)` 返回 `{"buttons":(18,21), "j_left":(18,2), "j_right":(18,2)}`。
- 17 键映射：`MODEL_BUTTONS` 21 键中取 `MODEL17_IDX` 对应 17 键（evaluate.py）。
- **差异定义 D（自拟）** = 按键不一致位数 + 双摇杆平均 L2/√2；Top-5 帧按 D 降序（/api/sequences）。
- **微调数据形态（第 6 天实测）**：
  - `tokenizer.pack_actions` 需 3D `[chunks=1, T=18, dim]`（action_horizon=18、action_dim=25=21键+摇杆×2）；单样本 buttons `[1,18,17]`、j_left/j_right `[1,18,2]`。
  - `model.forward` 的 vl_token_ids/sa_token_ids/actions/actions_mask 须送 CUDA（否则 masked_scatter device mismatch）。
  - `tokenizer.encode` 校验 `actions.shape[0]==action_horizon(18)`，**batch 须逐样本 encode 再堆叠**。
- `/api/metrics?game=&video=`（shift 可选）→ 指标条数据；`REFERENCE = {acc_17keys:0.50, corr_jl:0.40, test_frames:200}`。

## 四、评估指标口径（重要，报告必须正确引用）

- 按键一致率**主口径 = B（逐帧全对：17 键全一致才算该帧对）**，`acc_17keys_all`；A 口径（逐键逐帧）存 `acc_17keys_bits`。
- 参考水平：按键约 50%（B 口径）、摇杆相关 0.40。
- 实测（B 口径）：lies_of_p 41.9% / k=4，hades 41.5% / k=6；摇杆相关均 <0.1 未达标。
- **摇杆相关接近 0 是模型真实行为**（8 方向命中率 14.9~18.7% ≈ 随机 12.5%；IDLE 帧也漂移），非评估 bug。
- 扩展 B（IDLE 过滤）结论：误触发率过滤口径（A 方案）效果不稳定（-18%/+2.6%/+62% 三种测试集），目标 -20% 未达成；路径 B（抑制输出）方案已记录未实现（用户确认先不做）。

## 五、当前进度（第 6 天，2026-08-24）

- ✅ 环境/冒烟/Web 平台/两游戏评估全部就绪；hades、lies_of_p 已测（B 口径）。
- ✅ Harness 三件套（开发规范/日志/run_checks.py）建立，本地自测通过（run_checks 3 项 PASS）。
- ✅ `finetune.py` 最小可跑链路打通（--samples 2 实测：avg_loss=1.0086，FINETUNE DONE，保存 ng_finetuned.pt）。
- ⏳ 第 6 天报告已写（`第6天报告.md`，不入库）。
- 🔲 未提交：`scripts/finetune.py`（未验证完成，等用户指示）。

## 六、遗留事项（第 7 天起）

1. **evaluate.py 不支持 `--ckpt`** → 微调权重无法评估对照；需加 `--ckpt` 参数出 zero-shot vs 微调对照表。
2. finetune.py 仅 2 样本验证，需样本量扫描（1k/5k/10k 帧）找最小样本下 acc 提升最佳值。
3. `ng_finetuned.pt` 未验证能否被 load_model 重新加载。
4. 第 3 个"已测"游戏（star_fox_64/wreckfest）评估。
5. YouTube 下载 IP 级 bot 验证：换网络 / cookies.txt / 换源（无免登录方案）。

## 七、课程报告格式要点（第 6 天起）

- 报告结构：基本信息 → 今日目标（对应差距清单）→ 今日完成的两块（每块写：工作内容+对应需求、改动范围、自测步骤与结果）→ 交叉阅读/互查 → 今日完成与自检 → 问题计划与 AI 沟通。
- 两块必须"可独立说明、可自测"，自测要有**步骤与结果**，禁止只写"编写了大量代码"。
- 篇幅 800~2000 字；不得写密钥/仓库地址/录屏外链；不得把未完成写成已完成。
