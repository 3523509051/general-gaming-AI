---
description: 通用游戏智能体课题核心规则：项目目标、三步走工作流、提交与入库边界、关键事实约束。每次对话必须遵守。
alwaysApply: true
enabled: true
updatedAt: 2026-08-24T22:00:00.000Z
---

# 项目核心规则（通用游戏智能体 · 课题七）

## 一、项目目标

基于 NVIDIA **NitroGen**（500M 参数 DiT，flow-matching 行为克隆，训练于 4 万小时/1000+ 游戏手柄标注）做 zero-shot 推理、单游戏数据统计与定量评估，并完成前后端 Web 可视化平台。主路径：环境搭建 → ng.pt 推理 → 数据提取 → 评估 → Web 平台三 Tab 演示。

必做范围：① 跑通官方 ng.pt 推理可复现；② 同一游戏标注 ≥500 帧的按键/摇杆分布统计 + ≥10 条序列可视化；③ 测试集（默认 200 帧，前端可调 10~5000）报告按键一致率、摇杆 MSE/相关系数；④ zero-shot 基线对比；⑤ 演示；⑥ 归档与结课大报告。

## 二、三步走工作流（第 6 天起强制）

任何功能改动按 **计划 → 执行 → 核对** 推进：

1. **计划**：写清改哪些文件、完成哪条验收、如何自测（写入 `docs/开发日志.md` 当日条目）。
2. **执行**：只在计划列出的文件范围内修改；不得改无关模块；不得擅自引入未比选的新依赖。
3. **核对**：运行自测命令；对照预期；失败则带报错回到计划，收紧范围或改写验收标准后再试。

## 三、范围控制四要素

每次改动必须四要素齐备：**约束**（不改无关模块、不擅自引入未比选依赖）、**需求**（可验证的验收标准，不用形容词）、**范围**（允许改的文件/目录列表）、**输出**（自测命令 + 通过标准）。禁止写"继续完善""优化体验"式任务。

## 四、提交与入库边界（非常重要）

- **只提交与项目核心功能相关的文件**（`scripts/` 业务脚本、`web/` 前端、`README.md`、`requirements.txt`、`.gitignore`、`.codebuddy/rules/`）。
- **一律不入库**（已在 .gitignore 覆盖）：课程过程文档（`*报告.md`、`*交接清单.md`、`项目备忘.md`、`*解读.md`、`*实施文档.md`、`docs/审查纪要.md`、`docs/开发规范.md`、`docs/开发日志.md`、`scripts/run_checks.py`、`scripts/finetune.py` 未验证前不提交）、`data/` 全部、`NitroGen/`（含 ng.pt、.venv）、`.env`/`cookies.txt`、`assets/`。
- Harness 过程文件（开发规范/日志/run_checks）为本地开发基座，**不提交远程**（用户明确决定）。
- 小步提交：每个 commit 对应一件可验证改动；message 写清"改了什么 + 为何改"；**自测通过后再提交**，未通过不提交。
- 提交前用 `git status --porcelain` 核对暂存文件清单，**不得把课程文档/报告误 add**。

## 五、关键事实约束（写代码/报告前先确认）

- 标注为 **AI 提取非严格真值**（论文原话 "not strictly ground truth"）；模型是 System-1，只看最后一帧，单帧推理约 0.3s。
- 评估**主口径 = B（逐帧全对）**，`acc_17keys_all` 即此口径；A 口径（逐键逐帧）存 `acc_17keys_bits` 作对照。参考水平：按键一致率约 50%（B 口径）、摇杆相关 0.40。
- 已知结论：按键指标达标（0.95~0.96 A 口径 / ~0.42 B 口径），摇杆相关接近 0 未达标——**这是模型真实行为，不是评估 bug**，报告中如实描述，不得虚构达标。
- 报告/回复中文；生成中文内容注意乱码（Windows 命令行用 UTF-8，脚本内 `sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8", errors="replace")`）。

## 六、代码与数据路径约定

- venv Python：`NitroGen\.venv\Scripts\python.exe`（flask/torch/nitrogen/polars 都装在此）。系统 python 用于 scan_shard/init_db/probe（yt-dlp 等）。
- 数据根：`data/`（不入库）。每游戏 `data/<game>/`（manifest.json、annotations.parquet、test_set_<video>.csv、eval/、stats/、test_frames/）。
- 视频：`data/videos/<game>_<video>.mp4`；切片：`data/shards/SHARD_*.tar.gz`；结果库：`data/eval_results.db`。
- 权重：`NitroGen/ng.pt`（1.84GB）；微调输出：`NitroGen/ng_finetuned.pt`。
- 关键依赖：transformers **必须 4.57.1**（5.x 破坏性变更），torch 按显卡自适应（RTX 50 系 cu128）。
- 涉及 NitroGen 源码接口（tokenizer.encode/pack_actions/load_model 等）改动前，先读 `NitroGen/nitrogen/mm_tokenizers.py` 与 `flow_matching_transformer/nitrogen.py` 确认 shape 约定（详见 project-intel 规则）。

## 七、环境与运行

- 启动 Web：双击 `scripts/start_web.bat`（推荐）或 `powershell -ExecutionPolicy Bypass -File scripts/start_web.ps1`；后端监听 `http://localhost:5000`。
- 评估：`NitroGen\.venv\Scripts\python.exe scripts\evaluate.py --game <game> --video <video> --fps <fps>`（默认 200 帧纯随机，B 口径，帧数不符自动重建）。
- 自测基线：`NitroGen\.venv\Scripts\python.exe scripts\run_checks.py`（本地工具，不入库）。

## 八、禁则

- 不一次生成"剩余全部功能"——失败无法定位。
- 自测不得只有截图，必须有可重复命令/脚本。
- 不得把未做功能写进文案（界面/报告/README）；未验证的脚本（如 finetune.py 未完成对照实验）不提交、不写"已提升 acc"。
- 不主动 git add/commit/push，**等用户明确说"提交"**。
