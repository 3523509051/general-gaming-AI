# 项目备忘（第 3 天启动）

> 按指导书要求，自第 3 天起维护：记录技术选型、接口约定、待决问题、关键事实与实验结果。

## 一、技术选型记录（已定案）

- **方案 A「官方同构 · 最小依赖」（入选）**：polars + `InferenceSession` 直调 + `actions_raw.parquet` + matplotlib
- **方案 B「通用生态 · 可移植交互」（对照组，未选）**：pandas + serve.py + `actions_processed.parquet` + plotly/streamlit
- 语言：Python ≥3.10（官方强绑定，不做比选）
- 详见《第 2 天报告.md》技术选型章节

## 二、接口约定（评估脚本）

- **输入**：`data/hades/test_set.csv`（200 帧：画面路径 + 标注真值）
- **推理**：每帧 → `InferenceSession.predict()` → 18 步动作块（21 键 + 左右摇杆）
- **对齐**：动作块第 k 步与该帧标注真值对比，shift 全扫描 0~17
- **输出**：
  - `data/hades/eval/metrics.csv`（全 shift 指标表）
  - `data/hades/eval/predictions.csv`（最优 shift 下每帧预测 vs 真值对照）
  - `data/hades/eval/shift_scan.png`（shift 扫描曲线图）
- **指标口径**：按键一致率（17 键主口径 + 16 键论文口径）、摇杆 MSE/Pearson 分轴、全帧/过滤 IDLE 双口径

## 三、实验结果：shift 扫描（2026-08-20，Hades zero-shot）

**背景**：论文自承标注有延迟（overlay 软件引入小延迟 + 解析有误差）。为找到模型预测与标注最吻合的帧偏移，对动作块 18 步全量扫描。

**关键结果**：

| 指标 | 最优 shift | 数值 | 结论 |
| --- | --- | --- | --- |
| 按键一致率 17 键（全帧） | k=6 | **0.954** | 远超参考 0.50，达标 |
| 按键一致率 17 键（过滤 IDLE） | k=6 | 0.944 | 达标 |
| 按键召回率（真值按键被命中） | k=11 | **0.566** | 延迟对齐后命中率显著提升 |
| 按键精确率 | k=10 | 0.438 | 与召回率峰值基本同步 |
| 左摇杆相关 x | k=6 | **-0.008** | 远低于参考 0.40，未达标 |
| 左摇杆相关 y | k=6 | **-0.011** | 未达标 |
| 左摇杆 MSE x / y | k=6 | 0.609 / 0.349 | — |

**核心结论**：
1. **按键最佳对齐点 k=11~12 步**（约 0.37 秒 @30fps）——印证论文自承的标注延迟（overlay 延迟 + 解析误差叠加）。
2. **摇杆相关对 shift 不敏感**：k=0~17 全程在 ±0.03 内波动，无任何 shift 能使相关变正——说明摇杆预测接近随机，是无条件模式 + zero-shot 的弱项，而非对齐错误。
3. **达标判定**：按键指标达标（0.954 > 0.5），摇杆指标未达标（-0.008 < 0.4）。符合"测到多少如实报告"的口径。

**生成材料**：`scripts/plot_shift_scan.py`、`data/hades/eval/shift_scan.png`（可直接用于第 5 天演示）。

### 跨游戏验证：lies_of_p（2026-08-20，最小验证）

**背景**：为验证结论的泛化性（非 Hades 特例），另测一款游戏。探测候选（cuphead / resident_evil_4 / celeste / dark_souls / mortal_shell / lies_of_p）后发现仅 **lies_of_p v2276819038** 视频可用（6 分钟，60fps，1280×720，xboxone，完全覆盖其 18 chunks = 21,600 帧标注）。

**结果（199 帧，最优 shift k=7）**：

| 指标 | lies_of_p | Hades（对比） | 参考水平 |
| --- | --- | --- | --- |
| 按键一致率 17 键（全帧） | **0.960** | 0.954 | 0.50 ✅ |
| 按键一致率（过滤 IDLE） | 0.957 | 0.944 | 0.50 ✅ |
| 按键召回率 | 0.528 | 0.434 | — |
| 按键精确率 | 0.308 | 0.388 | — |
| 摇杆相关 x | **0.129** | -0.008 | 0.40 ❌ |
| 摇杆相关 y | 0.009 | -0.011 | 0.40 ❌ |
| 摇杆 MSE | 0.441 | 0.609 / 0.349 | — |
| IDLE 帧占比 | 20.1% | 39% | — |

**核心结论**：
1. **跨游戏一致性成立**：两游戏按键一致率均稳定 0.95~0.96（达标）；摇杆相关均未达标（0.00~0.13 < 0.4）。评估非单游戏特例。
2. **摇杆相关 lies_of_p（0.129）明显优于 Hades（-0.008）**：虽仍低于参考，但至少为正相关。可能原因：魂类游戏移动节奏较平缓（非速通高动态）、IDLE 帧更少（20% vs 39%）。
3. **标注延迟普遍性**：lies_of_p 按键召回率峰值在 shift 8~12 步，与 Hades（shift 11）基本一致——印证"标注延迟约 0.37 秒"是模型评估的普遍特性，非单游戏偶发现象。
4. **诚实口径**：两游戏均呈现"按键达标、摇杆未达标"的一致模式，符合"测到多少如实报告"。

**生成材料**：`scripts/tmp_eval_lies.py`（最小验证评估脚本，含抽帧+推理+shift 扫描）。

## 四、Web 可视化平台（2026-08-21 施工完成）

**技术栈**：Flask（venv Python）+ 原生 HTML/JS + ECharts（离线包）+ SQLite 结果库 + Parquet 文件层。设计详见《网页可视化平台实施文档.md》。

**已交付**（对应实施文档里程碑第 3 天下午 + 第 4 天内容，全部提前完成）：
- `scripts/scan_shard.py`：一键识别切片（89 游戏 311 视频 → `games_scan.json`）
- `scripts/probe_videos.py`：视频可用性探测（yt-dlp，输出 `video_status.json`，可中断续跑）
- `scripts/init_db.py`：建 `eval_results.db` + 种子数据
- `scripts/evaluate.py`：**通用化评估**（`--game/--video/--fps`，自动构建测试集→推理→shift 扫描→写库→补统计图）；predictions.csv 新增 `pred_*` 17 键明细列（支撑差异帧展示）
- `scripts/app.py`：Flask 后端 7 接口（games/videos/frame/stats/sequences/testset/rescan）+ LineageError 血缘断言 + InferenceSession 懒加载单例（线程安全）
- `web/`：三 Tab 前端（① 模型识别：帧滑条+手柄可视化+18 步摇杆轨迹；② 统计分布：按键柱状图+摇杆散点+摘要+静态图；③ 序列对比：预测 vs 真值曲线+差异 Top-5 跳转）
- `scripts/start_web.ps1`：一键启动（停旧实例→起服务→开浏览器）

**接口验证结论**（全部实测通过）：游戏列表 89 个、血缘断言 400 分支（错配 video 明确报错）、实时推理 fresh=1（首次含模型加载 10s，单帧 ~0.3s）、缓存模式 fresh=0、统计（29,400 帧 polars 实时计算）、序列 Top-5（含差异键明细）、rescan 后台探测（hades 5 视频：1 downloaded + 4 dead）。

**评估结果刷新**（evaluate.py 通用版重跑，与此前 tmp 脚本口径一致）：
- hades v1805686899：acc17=0.952，best shift k=7（此前 0.954/k=6，shift 6/7 段 acc 持平属正常浮动）
- lies_of_p v2276819038：acc17=0.961，best shift k=6（此前 0.960/k=7，同上）
- 两游戏均已在 `eval_results.db` 中（前端"已测"徽标数据源）

**关键工程事实**：
- chunk_size 因视频而异（hades v1805686899 为 600 帧/chunk=30fps×20s，其余视频 1200=60fps×20s），**不可写死**；fps 由 chunk_size/20 推导或 `--fps` 显式传入
- HF 联网检查拖慢模型加载（重试 5 次 ~1 分钟），设 `HF_HUB_OFFLINE=1` 后秒级加载（权重已缓存）
- app.py 必须用 venv Python 跑（flask+torch+nitrogen+polars 均装在 venv）；系统 Python 跑 scan_shard/init_db/probe

## 五、扩展 C 差异定义（已落地平台，2026-08-21；2026-08-21 更新为双摇杆同等地位）

**综合差异分**：`D = 按键不一致位数 + 双摇杆平均 L2/√2`
- 按键分量：17 键逐位比对的不一致个数（0~17），每键等权
- 摇杆分量：左右摇杆各算 `L2 = √((Δx)²+(Δy)²)`（预测 vs 真值），**取平均**后除以 √2 折到 [0,1]（对角最大距离）——右摇杆与左摇杆同等地位，不因数据稀少而忽略
- **Top-5**：按 D 降序取差异最大的约 5 帧（接口 `/api/sequences` 返回 `diff_score`/`jl_l2`/`jr_l2`）
- **约 20 段曲线**：测试集按 absolute_frame 排序后每 10 帧为一段（200 帧 → 20 段），每段返回均值 D 与最大差异帧；前端 Tab③ 有分段柱状图 + 差异曲线（可选 D 曲线并标 Top-5 红点）
- **统计分布（Tab②）**：左/右摇杆各一张位置分布图（同等地位）；shift 扫描图含左/右摇杆相关曲线（右摇杆数据全 NaN 的游戏自动跳过并说明）
- 实测 lies_of_p：198 帧 → 20 段，Top-1 帧 15561（n_mismatch=2、左L2=1.794、右L2=0.002、D=2.635）
- 说明：论文未提供加权公式，此定义为自拟（按键等权 + 双摇杆平均归一化同量级），报告引用时注明"自拟定义"

## 六、待决问题

1. **摇杆相关接近 0 的根因**：无条件模式（`game_mapping_cfg=None`，本分片无 game_label）vs zero-shot 泛化 vs Hades 高动态场景——三者如何归因？需在结课报告分析。
2. **IDLE 过预测**：模型在 33/78 IDLE 帧（42%）预测了按键但真值为 0（null action 过预测，论文已预警）。评估双口径已部分缓解，但根因未解决。
3. **测试集抽样**：当前 200 帧（种子 42）偏 IDLE（39%），如需更均衡的测试集可调整抽样策略。

## 七、关键事实（已确认）

- **标注为 AI 提取**（SegFormer 分割模型从视频 overlay 提取），**非严格真值**（论文原话 "not strictly ground truth"）。
- **模型能力边界**：System-1 快速反应模型，只看最后一帧，无长时记忆/规划；单帧推理实测 ~0.3s。
- **视频源状态**：HK 6 视频全失效；Hades 5 视频中仅 v1805686899 可下载（17 分钟，覆盖 chunk 0~980s），其余 4 个失效。
- **数据统计**（Hades 全量 538,200 帧）：south 触发率 19.0%（最高）、右摇杆完全未使用（0%）、左摇杆移动率 63.8%、IDLE 率 30.4%。

## 八、未来方向（假设，不动立项书）

- **实机游玩 vs 主播视频数据对比**（课后/后期可选探索）：AI 实机动作输出 vs 官方标注的统计层面对比，可作为扩展 C 可视化工具试炼场。仅统计层面可比，逐帧对照不成立。
